{
    email {$ADMIN_EMAIL:your-email@example.com}
}

{$DOMAIN:localhost} {
    # Reverse proxy to the SearchInlet Docker container
    reverse_proxy searchinlet:8080

    # Optional: Log requests
    log {
        output file /var/log/caddy/access.log
    }

    # Enable compression
    encode gzip
}
